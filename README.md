# ai-pharmacy-automation-suite
Microservice-based automation suite for pharmacy workflows. Orchestrates a headless browser gateway (FastAPI + Playwright) to interface with legacy portals and an AI agent (OpenAI API) for intelligent data parsing and decision making.
